# Home Assistant add-on profile. The gateway requires no host, Docker, hardware,
# Supervisor, or Home Assistant API access.
#include <tunables/global>
profile node_red_mcp_hub flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  network inet stream,
  network inet6 stream,
  capability setuid,
  capability setgid,
  /app/** rix,
  /data/options.json r,
  /run/node-red-mcp-hub/** rw,
}
